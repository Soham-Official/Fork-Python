https://practice.geeksforgeeks.org/problems/slicing-in-string-python/1/?track=python-module-3&batchId=119

def join_middle(bound_by, tag_name):
  # complete the statement below to return the string as required
  x=len(bound_by)
#   print(x)
  x=x//2
  return bound_by[0 : x  ] + tag_name + bound_by[ x:: ]
