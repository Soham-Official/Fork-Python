https://practice.geeksforgeeks.org/problems/dictionary-in-python-ii/1/?track=python-module-6&batchId=119


def pair_sum(dict, N, arr, sum):
    
    # Your code here
    # Hint: You can use 'in' to find if any key is in dict
    for i in arr:
        for j in range(1,len(arr)):
            if (i+arr[j]==sum):
                return True
    
    return False
